#include <stdio.h>

int main() {
    //prb 1 A Codeforces: Theatre Square
    long long n, m, a;
    scanf("%lld %lld %lld", &n, &m, &a);

    // calculate the number of flagstones needed along the length and width
    long long 
}